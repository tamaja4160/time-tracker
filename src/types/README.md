# Shared types

Cross-layer TypeScript types and adapter interfaces: `LogEntry`, `TimerStatus`,
`TimerState`, `TargetSheet`, `Result<T>`, and the interface declarations for
`TimerEngine`, `ActivityLogService`, `LogStore`, `Clock`, `AuthClient`, and
`GoogleSheetsConnector`. Populated in task 1.2.
